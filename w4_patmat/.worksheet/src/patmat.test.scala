package patmat

import Huffman._

object test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(75); val res$0 = 
	times(List('a', 'b', 'a'));System.out.println("""res0: List[(Char, Int)] = """ + $show(res$0))}
}
